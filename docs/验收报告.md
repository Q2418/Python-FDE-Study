# 项目验收报告

> 验收对象：企业人员资产管理后台系统（FDE 培训实训项目，阶段1~5）
> 验收时间：2026-09-20
> 验收方式：pytest 全量单元测试 + 阶段3/4/5 接口冒烟脚本（自动化执行）

## 一、验收环境

| 项目 | 版本/说明 |
| --- | --- |
| 操作系统 | Windows |
| Python | 3.11.9 |
| FastAPI / Starlette | 0.141.1 / 1.6.0 |
| SQLAlchemy / Pydantic | 2.0.54 / 2.13.5 |
| 数据库 | SQLite（默认，可一行配置切换 MySQL 8.0） |
| 服务地址 | http://127.0.0.1:8000（本地运行中） |

## 二、验收结果总览

| # | 验收套件 | 检查项 | 结果 |
| --- | --- | --- | --- |
| 1 | 单元测试（pytest 全量） | 62 个用例 | 全部通过 |
| 2 | 阶段3 接口冒烟（登录/权限/领用归还/记录） | 21 项 | 全部通过 |
| 3 | 阶段4 接口冒烟（附件/导出/高级查询/AI） | 15 项 | 全部通过 |
| 4 | 阶段5 接口冒烟（提示词/项目RAG/Skills/工作流） | 17 项 | 全部通过 |
|  | **合计** | **115 项** | **115 通过 / 0 失败** |

## 三、单元测试明细（62 个用例）

| 测试文件 | 覆盖内容 |
| --- | --- |
| `test_auth.py` | 登录成功/失败、令牌校验、未登录 401、普通员工写操作 403 |
| `test_employee.py` | 员工增删改查、必填/手机号/邮箱/状态校验、重复工号拦截、分页筛选、404 |
| `test_asset.py` | 资产增删改查、重复编号/名称拦截、价格与状态校验、领用人清空 |
| `test_record.py` | 领用/归还流程、重复领用拦截、未领用归还拦截、离职员工拦截、记录筛选分页 |
| `test_employee_extra.py` | 多条件筛选、日期范围、白名单排序、附件上传/下载/删除、类型与大小拦截、Excel 导出 |
| `test_ai.py` | 知识库文档上传/列表/删除、RAG 问答、空知识库兜底、工具调用（员工/资产/记录） |
| `test_ai_engine.py` | 提示词列表/热重载/变量渲染、Skills 列表与执行、文件读取沙箱与路径穿越拦截、项目知识库重建、Agent 工作流全流程与历史记录 |

## 四、接口冒烟明细

### 阶段3（21 项）
页面可访问（6）、管理员/员工登录与角色、密码错误 401、未登录拦截 401、带 Token 查询、领用成功与状态变更、重复领用拦截、归还成功与状态还原、未领用归还拦截、领用记录留痕、普通员工写操作 403、记录只读可查。

### 阶段4（15 项）
登录、排序查询、状态筛选、附件上传、附件下载内容一致、Excel 导出（xlsx 二进制）、知识库文档上传/列表、RAG 问答（答案 + 来源）、工具调用（员工/资产/记录）、文档删除、附件清理、AI 页面可访问。

### 阶段5（17 项）
提示词固化列表（v1.1/v1.0）与热重载、项目知识库重建（37 文件 / 163 切片）、项目 RAG 问答命中来源、5 个 Skills 注册、表结构技能、文件读取技能、路径穿越拦截、对话自动调用技能（表结构/文件）、Agent 工作流（6 步 / 评审轮次）、3 个真实需求跑通、工作流历史记录、AI 页面可访问。

## 五、功能验收清单（对照各阶段作业）

| 阶段 | 验收点 | 结果 |
| --- | --- | --- |
| 阶段1 | 建表 SQL、FastAPI 项目、前后端 CRUD、正常启动 | 通过 |
| 阶段2 | 统一返回、全局异常中文提示、参数校验、分页筛选、单测、Postman | 通过 |
| 阶段3 | 用户/角色/关联表、JWT 登录、权限控制、登录页、领用归还流程、记录追溯 | 通过 |
| 阶段4 | 附件上传（类型/大小/路径管理）、Excel 导出、高级查询、日志、RAG 问答、工具调用、超时异常、部署脚本 | 通过 |
| 阶段5 | 提示词固化（两套标准 Prompt + 版本）、项目专属 RAG、Skills 工具（数据库 + 文件读取）、Agent 工作流（六步 + 防护 + 记录） | 通过 |

## 六、说明与已知限制

1. **AI 模式**：当前未配置 `AI_API_KEY`，使用内置模拟模式；接口、流程、防护、记录均真实运行，配置 key 后自动切换真实大模型（`backend/app/config.py` 一行）。
2. **RAG 检索**：采用字符二元组相似度（免向量库依赖），已验证可用；语义级检索升级方案已记录在 `docs/阶段5-AI作业记录.md`。
3. **前端依赖**：Vue/Element Plus/Axios 已本地化（`static/lib/`），离线环境可正常访问，无外部 CDN 依赖。
4. **GitHub 推送**：网络波动时需重试（本项目提交始终本地安全留存，已全部同步远程）。

## 七、验收结论

- 全部 115 项自动化检查通过，5 个阶段功能完整、流程闭环（登录 → 业务操作 → 领用归还 → 记录追溯 → AI 问答 → Agent 自动开发）。
- 项目可一键部署、可切换数据库、带完整交付文档与过程记录，**达到验收标准**。

验收执行命令（可复现，脚本已随仓库提供）：

```bash
cd backend
python -m pytest tests -v          # 62 个单元测试
python scripts/phase3_smoke.py     # 阶段3 冒烟（21 项）
python scripts/phase4_smoke.py     # 阶段4 冒烟（15 项）
python scripts/phase5_smoke.py     # 阶段5 冒烟（17 项）
python scripts/acceptance.py       # 一键全量验收（单测 + 全部冒烟）
```

> 说明：冒烟脚本需先启动服务（`python run.py`），默认地址 http://127.0.0.1:8000。
